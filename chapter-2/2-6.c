#include <stdio.h>
#include <stdlib.h>

/* Problem:
 * Write a function setbits(x, p, n, y) that returns x wht the n bits that
 * begin at position p set to the rightmost n bits of y, leaving the other
 * bits unchanged. 
 */

int main(void){

}
